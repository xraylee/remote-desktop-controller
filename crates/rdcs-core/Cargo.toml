[package]
name = "rdcs-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared types and error definitions for the RDCS remote desktop system"

[dependencies]
thiserror = { workspace = true }
