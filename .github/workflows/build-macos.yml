name: Build macOS Client

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-macos:
    strategy:
      matrix:
        include:
          - runner: macos-14
            arch: arm64
            rust-target: aarch64-apple-darwin
            flutter-arch: arm64

          # WARNING: macos-13 runner will be deprecated in Q4 2026
          # Plan migration to self-hosted Intel runner or drop x64 support
          - runner: macos-13
            arch: x64
            rust-target: x86_64-apple-darwin
            flutter-arch: x64

    runs-on: ${{ matrix.runner }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-target }}

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "."

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Get Flutter dependencies
        run: |
          cd client/flutter
          flutter pub get

      - name: Build Rust FFI
        run: |
          cargo build --release \
            --target ${{ matrix.rust-target }} \
            --package rdcs-ffi

      - name: Build Flutter app
        run: |
          cd client/flutter
          flutter build macos --release \
            --target-arch=${{ matrix.flutter-arch }}

      - name: Verify library install_name
        run: |
          DYLIB="client/flutter/build/macos/Build/Products/Release/rdcs_client.app/Contents/Frameworks/librdcs_core.dylib"
          echo "Verifying install_name for ${{ matrix.arch }}..."
          otool -D "$DYLIB"
          INSTALL_NAME=$(otool -D "$DYLIB" | tail -1)
          if [ "$INSTALL_NAME" != "@rpath/librdcs_core.dylib" ]; then
            echo "❌ install_name verification failed: $INSTALL_NAME"
            exit 1
          fi
          echo "✅ install_name correct: $INSTALL_NAME"

      - name: Package DMG
        run: ./scripts/package_macos.sh ${{ matrix.arch }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: rdcs-client-macos-${{ matrix.arch }}
          path: rdcs_client-${{ matrix.arch }}-*.dmg

      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: rdcs_client-${{ matrix.arch }}-*.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
